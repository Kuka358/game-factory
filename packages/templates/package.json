{
  "name": "@game-factory/templates",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@game-factory/game-spec": "workspace:*",
    "@game-factory/runtime": "workspace:*",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "vitest": "^3"
  }
}